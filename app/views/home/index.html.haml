%h1 Hello Dad!

=if !current_user.nil?
  %h1
    I,
    =current_user.first_name
    #{current_user.last_name}, am logged in!
  %br
  =current_user.email
  %br
  =link_to "logout", logout_path, method: :delete
-else
  =link_to "signup", signup_path
  =link_to "login", login_path